WARNING: the .jar file will fail to load the background image and background music if it is not ran directly inside the RobloxLauncher file.
If you need to move the .jar somewhere, make a shortcut (windows) or an alias (mac) instead of moving the file.
